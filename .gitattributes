# Normalize line endings: store text as LF in the repo, check out native.
* text=auto

# Keep PowerShell scripts CRLF on checkout (Windows tooling)
*.ps1 text eol=crlf

# Treat font/bitmap headers as text (they are C headers)
*.h text
